<h5>Name:<%= @conference_room.name %></h5>

<h5>Available:<%= @conference_room.available %></h5>

<%= link_to ' Edit', edit_conference_room_path(@conference_room), :class=>"fa fa-pencil-square-o btn btn-primary"%> 
<%= link_to ' Back', conference_rooms_path, :class=>"fa fa-arrow-left btn btn-primary" %>
<%= link_to ' Destroy', @conferenceroom, method: :delete, data: { confirm: 'Are you sure?' }, :class=>"fa fa-times btn btn-danger" %>
